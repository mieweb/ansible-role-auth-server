---
# defaults/main.yml
# You can override these in your playbook or inventory
root_url: "http://localhost"
mongo_url: "mongodb://localhost:27017/mieweb_auth_app"
mail_url: "smtp://localhost:25"
admin_email: "admin@example.com"
from_email: "auth@example.com"
ssl_cert_pem: "CHANGEME: PEM-encoded certificate goes here"
ssl_key_pem: "CHANGEME: PEM-encoded private key goes here"
nginx_upstreams:
  - "localhost"
